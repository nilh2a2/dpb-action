name: Base

on:
  workflow_call:
    inputs:
      environment:
        description: 'Environment'
        required: true
        type: string
        default: 'production'
      timeout-minutes:
        description: 'Job timeout in minutes'
        required: true
        type: number
        default: 60
      command:
        description: 'Command to execute (e.g., "newspaper exec", "sitemap-trends scan")'
        required: true
        type: string
      enable-sing-box-proxy:
        description: 'Start the container-local sing-box proxy'
        required: false
        type: boolean
        default: false
      google-trends-proxy-url:
        description: 'Proxy URL used only by the Google Trends client'
        required: false
        type: string
        default: ''

jobs:
  execute:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    timeout-minutes: ${{ inputs.timeout-minutes }}

    steps:
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull Docker image
        run: docker pull ghcr.io/nilh2a2/data-pipeline-bot:latest

      - name: Print image info
        run: |
          docker inspect ghcr.io/nilh2a2/data-pipeline-bot:latest \
            --format 'RepoTags: {{.RepoTags}}{{"\n"}}RepoDigests: {{.RepoDigests}}'

      - name: Print workflow inputs
        run: |
          echo "=== Workflow Inputs ==="
          echo "Trigger: ${{ github.event_name == 'schedule' && 'Scheduled run' || 'Manual dispatch' }}"
          echo "Environment: ${{ inputs.environment }}"
          echo "======================="

      - name: Run pipeline
        env:
          APP_ENVIRONMENT_DEV: ${{ inputs.environment == 'development' }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          APP_DB_PASSWORD: ${{ secrets.APP_DB_PASSWORD }}
          APP_EXTERNAL_SERVICE_APIFY_TOKEN: ${{ secrets.APP_EXTERNAL_SERVICE_APIFY_TOKEN }}
          APP_EXTERNAL_SERVICE_DATAFORSEO_LOGIN: ${{ secrets.APP_EXTERNAL_SERVICE_DATAFORSEO_LOGIN }}
          APP_EXTERNAL_SERVICE_DATAFORSEO_PASSWORD: ${{ secrets.APP_EXTERNAL_SERVICE_DATAFORSEO_PASSWORD }}
          APP_EXTERNAL_SERVICE_SERPAPI_API_KEY: ${{ secrets.APP_EXTERNAL_SERVICE_SERPAPI_API_KEY }}
          APP_EXTERNAL_SERVICE_SERPSTACK_ACCESS_KEY: ${{ secrets.APP_EXTERNAL_SERVICE_SERPSTACK_ACCESS_KEY }}
          APP_EXTERNAL_SERVICE_ZENSERP_API_KEY: ${{ secrets.APP_EXTERNAL_SERVICE_ZENSERP_API_KEY }}
          SING_BOX_CONFIG_JSON: ${{ inputs.enable-sing-box-proxy && secrets.SING_BOX_CONFIG_JSON || '' }}
          APP_EXTERNAL_SERVICE_GOOGLE_TRENDS_PROXY_URL: ${{ inputs.google-trends-proxy-url }}
        run: |
          docker run --rm \
            -e APP_ENVIRONMENT_DEV=$APP_ENVIRONMENT_DEV \
            -e GITHUB_RUN_ID=$GITHUB_RUN_ID \
            -e APP_DB_PASSWORD=$APP_DB_PASSWORD \
            -e APP_EXTERNAL_SERVICE_APIFY_TOKEN=$APP_EXTERNAL_SERVICE_APIFY_TOKEN \
            -e APP_EXTERNAL_SERVICE_DATAFORSEO_LOGIN=$APP_EXTERNAL_SERVICE_DATAFORSEO_LOGIN \
            -e APP_EXTERNAL_SERVICE_DATAFORSEO_PASSWORD=$APP_EXTERNAL_SERVICE_DATAFORSEO_PASSWORD \
            -e APP_EXTERNAL_SERVICE_SERPAPI_API_KEY=$APP_EXTERNAL_SERVICE_SERPAPI_API_KEY \
            -e APP_EXTERNAL_SERVICE_SERPSTACK_ACCESS_KEY=$APP_EXTERNAL_SERVICE_SERPSTACK_ACCESS_KEY \
            -e APP_EXTERNAL_SERVICE_ZENSERP_API_KEY=$APP_EXTERNAL_SERVICE_ZENSERP_API_KEY \
            -e SING_BOX_CONFIG_JSON \
            -e APP_EXTERNAL_SERVICE_GOOGLE_TRENDS_PROXY_URL \
            ghcr.io/nilh2a2/data-pipeline-bot:latest ${{ inputs.command }}
